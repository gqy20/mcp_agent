[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow
    integration: marks tests as integration
addopts =
    --tb=short
    --strict-markers
    --strict-config
    --ignore=tests/integration/test_crossplatform_mcp.py
